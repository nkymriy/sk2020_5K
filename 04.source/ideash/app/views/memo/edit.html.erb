<%= render :partial => 'shared/homemenu' %>

<div class="pusher">
  <div style="height: 15px"></div>
  <%# 右部のメモ一覧 %>
  <div class="ui card  right floated list" style="overflow: scroll;height: 100%;background: white;padding: 10px">
    <%# reverseで逆順に並べる %>
    <%= render partial: 'shared/memomenu', collection: @user_memo.reverse, as: "memo" %>
  </div>

  <div class="ui container">
    <div class="ui text">メモを確認しましょう。</div>
    <%= form_with model: @memo, class: 'ui form', local: true, url: "/idea/memo?id=#{@memo.id}" do |f| %>

  <div class="fields">
        <div class="sixteen wide field">
          <label>タイトル</label>
          <%= f.text_field :idea_name, placeholder: "メモに名前をつけてください", class: 'field',:required => true %>
        </div>
      </div>
      <div class="fields">
        <%= f.hidden_field :idea_category_id, :value => 1 %>
        <div class="sixteen wide field">
          <label>内容</label>
          <%= f.text_area :idea_description, placeholder: "メモを書きましょう", :required => true%>
        </div>
      </div>
      <%= f.submit '更新', class: "ui black button" %>
    <% end %>
  </div>
</div>


<%#= form_with model: @memo, local: true, url: idea_memo_path(@memo.id) do |f| %>

  <%# if @memo.errors.any? %>
<!--    <h3>入力した内容にエラーが<%#= @memo.errors.count %>件あります</h3>-->
<!--    <ul>-->
      <%# @memo.errors.full_messages.each do |message| %>
<!--        <li><%#= message %></li>-->
      <%# end %>
<!--    </ul>-->
  <%# end %>

<!--  <p>-->
    <%#= f.label :idea_name %>
    <%#= f.text_field :idea_name %>
<!--  </p>-->

<!--  <p>-->
    <%#= f.label :idea_description %>
    <%#= f.text_field :idea_description %>
<!--  </p>-->

  <%#= f.submit %>

<%# end %>